using System;
using System.IO;

namespace PrySec.Core.IO;

public static class StreamExtensions
{
    public static bool ReadLine(this Stream stream, Span<byte> buffer)
    {
        return false;
    }
}
